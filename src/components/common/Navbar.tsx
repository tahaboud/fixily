import { Box, Container, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <Container>
      <Box>
        <Typography>Logo</Typography>
        <Box>Links</Box>
      </Box>
    </Container>
  );
};

export default Navbar;
